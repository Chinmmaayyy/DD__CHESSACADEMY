/** Shared motion constants (typed for framer-motion). */

// Soft ease-out cubic bezier used across the site's entrances.
export const EASE_OUT: [number, number, number, number] = [0.22, 1, 0.36, 1]
